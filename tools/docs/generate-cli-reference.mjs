import { spawnSync } from "node:child_process";
import { readFileSync, writeFileSync } from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const rootDir = path.resolve(fileURLToPath(new URL("../..", import.meta.url)));

function parseArgs(argv) {
  const options = {
    output: "docs/cli-reference.md",
    input: "",
    check: false,
    stdout: false,
  };
  for (let index = 0; index < argv.length; index += 1) {
    const arg = argv[index];
    switch (arg) {
      case "--output":
        options.output = requireValue(argv, ++index, arg);
        break;
      case "--input":
        options.input = requireValue(argv, ++index, arg);
        break;
      case "--check":
        options.check = true;
        break;
      case "--stdout":
        options.stdout = true;
        break;
      default:
        throw new Error(`unknown argument: ${arg}`);
    }
  }
  return options;
}

function requireValue(argv, index, flag) {
  const value = argv[index];
  if (!value || value.startsWith("--")) {
    throw new Error(`${flag} requires a value`);
  }
  return value;
}

function loadCommandCatalog(options) {
  if (options.input) {
    return JSON.parse(readFileSync(path.resolve(rootDir, options.input), "utf8"));
  }
  const result = spawnSync("go", ["run", "./cmd/agent-testbench", "commands", "--all", "--json"], {
    cwd: rootDir,
    encoding: "utf8",
    stdio: "pipe",
  });
  if (result.status !== 0) {
    throw new Error(result.stderr || result.stdout || "failed to read command catalog");
  }
  return JSON.parse(result.stdout);
}

function renderCLIReference(report) {
  const commands = Array.isArray(report.commands) ? report.commands : [];
  const daily = commands.filter((command) => command.tier === "daily");
  const areas = groupBy(commands, (command) => command.area || "(none)");
  const lines = [
    "# AgentTestBench CLI Reference",
    "",
    "Generated from `agent-testbench commands --all --json`. Do not edit this file by hand; run `npm run docs:cli-reference` after changing CLI usage, tiers, or replacements.",
    "",
    `Total commands: \`${commands.length}\``,
    `Daily agent commands: \`${daily.length}\``,
    "",
    "## Daily Agent Surface",
    "",
    "| Command | Usage |",
    "| --- | --- |",
    ...daily.map((command) => `| \`${escapeMarkdown(command.command)}\` | \`${escapeMarkdown(command.usage)}\` |`),
    "",
    "## Command Areas",
    "",
    "| Area | Commands | Daily |",
    "| --- | ---: | ---: |",
  ];

  for (const area of Object.keys(areas).sort()) {
    const areaCommands = areas[area];
    lines.push(`| \`${escapeMarkdown(area)}\` | ${areaCommands.length} | ${areaCommands.filter((command) => command.tier === "daily").length} |`);
  }

  lines.push("");
  for (const area of Object.keys(areas).sort()) {
    lines.push(`### \`${escapeMarkdown(area)}\``, "");
    lines.push("| Command | Tier | Audience | Stability | Store | Replacement | Usage |");
    lines.push("| --- | --- | --- | --- | --- | --- | --- |");
    for (const command of areas[area]) {
      lines.push([
        `\`${escapeMarkdown(command.command)}\``,
        markdownText(command.tier),
        markdownText(command.audience),
        markdownText(command.stability),
        command.storeAware ? "yes" : "no",
        command.replacement ? `\`${escapeMarkdown(command.replacement)}\`` : "",
        `\`${escapeMarkdown(command.usage)}\``,
      ].join(" | ").replace(/^/, "| ").replace(/$/, " |"));
    }
    lines.push("");
  }
  return `${lines.join("\n").trimEnd()}\n`;
}

function groupBy(items, keyFn) {
  const groups = {};
  for (const item of items) {
    const key = keyFn(item);
    groups[key] ??= [];
    groups[key].push(item);
  }
  return groups;
}

function markdownText(value) {
  return escapeMarkdown(String(value || ""));
}

function escapeMarkdown(value) {
  return String(value || "")
    .replace(/\\/g, "\\\\")
    .replace(/\|/g, "\\|")
    .replace(/`/g, "\\`")
    .replace(/\r?\n/g, " ");
}

function main() {
  const options = parseArgs(process.argv.slice(2));
  const outputPath = path.resolve(rootDir, options.output);
  const rendered = renderCLIReference(loadCommandCatalog(options));
  if (options.stdout) {
    process.stdout.write(rendered);
    return;
  }
  if (options.check) {
    const current = readFileSync(outputPath, "utf8");
    if (current !== rendered) {
      throw new Error(`${path.relative(rootDir, outputPath)} is out of date; run npm run docs:cli-reference`);
    }
    console.log(`${path.relative(rootDir, outputPath)} is up to date`);
    return;
  }
  writeFileSync(outputPath, rendered);
  console.log(`wrote ${path.relative(rootDir, outputPath)}`);
}

try {
  main();
} catch (error) {
  console.error(error.message);
  process.exit(1);
}
